{
  "name": "lutask",
  "version": "0.1.0",
  "main": "main.js",
  "scripts": {
    "start" : "webpack && electron .",
    "build" : "electron-packager . lutask --all --version=0.33.3 --app-version=0.1.0 --out=dist --ignore=dist --prune"
  },
  "author": "UrakovRS"
}
